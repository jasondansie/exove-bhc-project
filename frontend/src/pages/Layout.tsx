import React from 'react';
import AdminConsole from '../app/AdminConsole';

const Layout = () => {
    return (
        <div>
            <AdminConsole />
        </div>
    );
};

export default Layout;