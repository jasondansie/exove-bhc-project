import React from 'react';
import PageHeading from './PageHeading';

const Logout = () => {
    return (
        <div>
            <PageHeading
                pageTitle="You can logout now"
            />
        </div>
    );
};

export default Logout;